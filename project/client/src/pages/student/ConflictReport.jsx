import { Navigate } from 'react-router-dom';

// Merged into the unified Incident Report page (RaggingReport.jsx with type toggle)
export default function ConflictReport() {
    return <Navigate to="/student/ragging" replace />;
}
